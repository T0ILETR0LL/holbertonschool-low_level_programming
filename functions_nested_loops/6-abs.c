#include "main.h"

/**
 * _abs - computes the absoloute value of an interger
 * @n: variable
 * Return: (1) positive, (0) zero, (-1) negative, (-98) 
 */
int _abs(int n)
{
	
	if (n < 0)
	{
		return (-n);
	}
	else 
	{
		return (n);
	}
}
