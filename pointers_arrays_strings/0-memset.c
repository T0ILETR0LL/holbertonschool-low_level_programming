#include "main.h"

/**
 * memset - fills memory with a constant byte
 * @s: string
 * Return: (0)
 */
char *_memset(char *s, char b, unsigned int n)
{
	while string n*size 
		fill with char b
		
	what is n?? 
	return (s)
}
